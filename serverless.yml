service: serverless-swapi
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1

functions:
  swapiPost:
    handler: routes/postHandler.handler
    environment:
      DATABASE_URL: ${self:custom.database_url}
    events:
      - http:
          path: swapi
          method: post

  swapiGet:
    handler: routes/getHandler.handler
    environment:
      DATABASE_URL: ${self:custom.database_url}
    events:
      - http:
          path: swapi/{model}
          method: get
plugins:
  - serverless-esbuild

custom:
  database_url: 'mysql://yhzoim9sdwz3j9mjqh1l:pscale_pw_tDrtzGAjqSUQ3yywmAKdv1PWX8lwBWO42exspcEm6Ro@aws.connect.psdb.cloud/proveedor?ssl={"rejectUnauthorized":true}'
