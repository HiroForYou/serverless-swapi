service: serverless-swapi
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1

functions:
  swapiPost:
    handler: routes/postHandler.handler
    environment:
      DATABASE_URL: ${self:custom.database_url}
    events:
      - http:
          path: swapi
          method: post

  swapiGet:
    handler: routes/getHandler.handler
    environment:
      DATABASE_URL: ${self:custom.database_url}
    events:
      - http:
          path: swapi/{model}
          method: get
plugins:
  - serverless-esbuild

custom:
  database_url: 'mysql://7uxbamoocfqj27lkbg4y:pscale_pw_7OzgA8ULWYu0MZgNqKrnsVNnMsm0RaPcg3qYEJfnMsw@aws.connect.psdb.cloud/proveedor?ssl={"rejectUnauthorized":true}'
